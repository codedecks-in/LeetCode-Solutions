#include <stdio.h>

int main ()
{
	scanf("%lld", &n);
	for(i=0 ; i<n ; i++)
	{
		scanf("%lld", &m[i]);
		if(i==0)
		{
			max=m[0];
		}
		else if(m[i]>max)
		{
			max=m[i];
		}
		pref
	}
	for(i=0 ; i<n ; i++)
	{
		scanf("%lld", &b[i]);
	}


	return 0;
}
